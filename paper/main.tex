%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Faithful-by-Concept — MICAD 2026 submission
% Springer Proceedings class (svproc). Compile on Overleaf with the MICAD
% "Template@Overleaf" project, or locally with svproc.cls + styles/ present.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\documentclass{svproc}

% to typeset URLs, URIs, and DOIs
\usepackage{url}
\def\UrlFont{\rmfamily}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{microtype} % improves justification / reduces overfull lines
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}

\begin{document}
\mainmatter
%
\title{Faithful-by-Concept: Verifiable and Equitable Concept Reasoning for Skin-Lesion Diagnosis}
%
\titlerunning{Faithful-by-Concept}
%
\author{B.~Aswathi\inst{1} \and Vidhyadharan~B.\inst{1} \and R.~Suganya\inst{1} \and
Girish~H.~Subramanian\inst{2} \and Nagesparan~Aimarappan\inst{3} \and
S.~Vijayaragavan\inst{4}}
%
\authorrunning{B. Aswathi et al.}
%
\tocauthor{B. Aswathi, Vidhyadharan B., R. Suganya, Girish H. Subramanian,
Nagesparan Aimarappan, S. Vijayaragavan}
%
\institute{Vellore Institute of Technology, Chennai, India\\
\email{aswathi.b2023@vitstudent.ac.in, vidhyadharan.b2023@vitstudent.ac.in,
suganya.r@vit.ac.in}
\and
Penn State University Harrisburg, Harrisburg, USA\\
\email{ghs2@psu.edu}
\and
UCSI University, Kuala Lumpur, Malaysia\\
\email{Nagesparan@ucsiuniversity.edu.my}
\and
Nelco (Tata Group), India\\
\email{vijaysarainn@gmail.com}}

\maketitle
%
\begin{abstract}
Dermatology AI increasingly claims to explain decisions through clinical concepts
such as the 7-point checklist and ABCD, yet rarely tests whether those concepts
\emph{causally} drive the decision, and never whether such faithfulness is equitable
across skin tones. We present Faithful-by-Concept, a concept-bottleneck framework
that diagnoses through dermatologist concepts (bootstrapped from a frozen dermatology
foundation model), introduces a concept-counterfactual faithfulness test that
intervenes in concept space---no image generator---and audits whether faithfulness
holds across Fitzpatrick skin tones. On derm7pt (5 seeds), the bottleneck matches an
image-only baseline for melanoma detection (AUROC $0.85\pm0.01$ vs.\ $0.86\pm0.03$)
while remaining interpretable, and with ground-truth concepts exceeds it ($0.92$).
A common ``leaky'' concept model appears interpretable yet is causally unfaithful
(concept reliance $\approx 0$ vs.\ $0.19\pm0.02$; Wilcoxon $p\ll10^{-3}$), and we show
rank-correlation faithfulness is magnitude-blind. Auditing across Fitzpatrick skin
tones, reasoning faithfulness is statistically \emph{equitable} (no significant
dark-vs-light disparity in any training regime), with neither training-diversity nor
post-hoc calibration producing a significant change. All experiments run on a single
GPU with frozen encoders.
\keywords{concept bottleneck, faithfulness, algorithmic fairness, dermoscopy,
explainable AI}
\end{abstract}
%
\section{Introduction}

Concept-based explanations are attractive for clinical AI because they speak the
clinician's language: rather than a heatmap, the model reports \emph{why} in terms a
dermatologist already trusts---an atypical pigment network, a blue-whitish veil,
irregular streaks. A growing body of dermatology work routes predictions through such
concepts and presents the concept activations as an explanation of the diagnosis.

But a concept explanation is only worth trusting if it is \emph{faithful}---if the
concepts it cites are the ones that genuinely determine the decision---and only worth
deploying if that faithfulness is \emph{equitable} across the patient groups the model
will serve. Both properties are routinely assumed and almost never tested.
Concept-bottleneck papers assert faithfulness by architecture; fairness studies in
dermatology measure \emph{accuracy} gaps across skin tone but never gaps in
\emph{reasoning}. The result is a literature of models that look interpretable and
look fair, with neither claim verified.

We close both gaps with three contributions.
\begin{enumerate}
\item \textbf{A concept-counterfactual faithfulness metric for dermatology.} Because
our diagnosis head consumes \emph{only} concepts, we intervene directly in concept
space---flip a concept from present to absent---and measure the causal effect on the
decision, with no generative model of images. We show that popular rank-correlation
faithfulness measures are magnitude-blind and can rate a concept-ignoring model as
faithful.
\item \textbf{Faithful-by-Concept}, a frozen-encoder concept bottleneck with concept
supervision bootstrapped from a dermatology foundation model where human labels do not
exist. Only two small heads are trained, and for the clinically-aligned detection task
it matches an image-only model at no interpretability cost.
\item \textbf{The first fairness-of-reasoning audit} in dermatology AI: we measure
concept faithfulness, not just accuracy, across Fitzpatrick skin tones, and find it
statistically equitable (no significant dark-vs-light disparity); we further report
that neither training diversity nor group-conditional calibration significantly changes
it (honest negatives).
\end{enumerate}
A leakage-clean protocol (lesion-level splits, embedding-based de-duplication)
underpins every result.

\section{Related Work}

\textbf{Concept-bottleneck and concept-alignment models.} Concept Bottleneck
Models~\cite{koh2020} predict human concepts then the label from them. In dermatology,
image--concept alignment~\cite{bie2024mica} and CLIP-based dermoscopic concept
predictors~\cite{patricio2024} provide concept explanations and test-time
intervention, but none \emph{tests} whether concepts are causally responsible, and
none examines fairness of the explanation.
\textbf{Post-hoc concept methods}~\cite{yuksekgonul2023} fit concept directions onto a
fixed classifier without certifying causal faithfulness.
\textbf{Foundation concept annotators} such as MONET~\cite{kim2024monet} score
concepts automatically; MONET is a labeling/auditing tool rather than a faithful
diagnostic system---we use this style of scoring to \emph{bootstrap} supervision.
\textbf{Counterfactual audits.} Pixel-level generative audits~\cite{degrave2023}
reveal shortcuts by editing images but need a generator and no concept vocabulary;
ours are inference-only in concept space.
\textbf{Faithfulness measurement} is long-studied~\cite{jacovi2020,adebayo2018};
ERASER~\cite{deyoung2020} operationalizes it via comprehensiveness and sufficiency,
which we adapt to concept space.
\textbf{Skin-tone fairness.} Daneshjou et al.~\cite{daneshjou2022} document accuracy
disparities across tone; conformal methods target fair coverage~\cite{lu2022}. All
concern accuracy or coverage---none measures whether \emph{reasoning} is equally
faithful across tones. To our knowledge, no prior work measures whether a skin-AI's
concept explanations are causally faithful, nor whether that faithfulness is equitable
across skin tones.

\section{Method}

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=5mm and 8mm,
  box/.style={draw,rounded corners,align=center,minimum height=9mm,inner sep=4pt,font=\small},
  frozen/.style={box,fill=black!6},
  trained/.style={box,fill=blue!8,draw=blue!55},
  lbl/.style={font=\footnotesize\itshape},
  arr/.style={-{Latex[length=2mm]},thick}
]
\node (img) [box] {Lesion\\image $x$};
\node (enc) [frozen, right=of img] {Frozen\\encoder $f$};
\node (ch)  [trained, right=of enc] {Concept\\head $g$};
\node (dh)  [trained, right=of ch] {Diagnosis\\head $h$};
\node (y)   [box, right=of dh] {label\\$\hat y$};
\draw[arr] (img) -- (enc);
\draw[arr] (enc) -- node[above,lbl]{$z$} (ch);
\draw[arr] (ch)  -- node[above,lbl]{$\hat c$} (dh);
\draw[arr] (dh)  -- (y);
\node (cf) [below=7mm of ch.south east, box, fill=red!6, draw=red!50, font=\footnotesize,
            text width=52mm] {\textbf{Counterfactual test:} flip a concept $\hat c_c\!\to\!1-\hat c_c$, re-run $h$, measure $|\Delta P(\hat y)|$};
\draw[arr,dashed,red!60] (cf.north) -- (dh.south);
\node[lbl,below=1mm of enc] {frozen};
\node[lbl,below=1mm of dh] {trained};
\end{tikzpicture}
\caption{Faithful-by-Concept. A frozen encoder produces an embedding $z$; a small
concept head predicts dermatologist concepts $\hat c$; the diagnosis head sees
\emph{only} $\hat c$. Because the decision passes through concepts alone, we can flip a
concept in concept space and measure its causal effect on the diagnosis (dashed).}
\label{fig:arch}
\end{figure}

\subsection{Two-model, shared-metric design}
Dermoscopic 7-point concepts describe magnified sub-surface structure and are not
present in ordinary clinical photographs, so a single concept model cannot honestly
span both domains. We instantiate the framework twice, sharing one architecture and
one faithfulness metric. \emph{Model A (dermoscopic)} trains on derm7pt with expert
7-point concept ground truth and validates the faithfulness metric against trustworthy
concepts. \emph{Model B (clinical)} trains on Fitzpatrick17k clinical photographs with
clinical concepts (ABCD + visible morphology) bootstrapped from a foundation model,
and hosts the fairness audit.

\subsection{Concept bottleneck}
Figure~\ref{fig:arch} summarizes the architecture. A frozen encoder $f$
(DermLIP~\cite{dermlip2025}; DINOv2~\cite{oquab2024} as ablation)
maps an image $x$ to an embedding $z=f(x)$. A concept head $g$ predicts concept
probabilities $\hat c=\sigma(g(z))\in[0,1]^K$; a diagnosis head $h$ predicts the label
from concepts \emph{only}: $\hat y = h(\hat c)$. Only $g,h$ (small MLPs) are trained;
embeddings are extracted once. The purity of the bottleneck ($h$ never sees $z$) makes
the counterfactual test causally meaningful. We train sequentially with class-balanced
cross-entropy and balanced-accuracy model selection. Where concept ground truth is
absent, we score concepts zero-shot from the foundation model with an ensemble of
positive/negative text prompts and use the probabilities as soft supervision.

\subsection{Concept-counterfactual faithfulness}
For a case with concepts $\hat c$ and predicted class $\hat y$ we compare the model's
\emph{stated importance} $I_c$ (gradient of $P(\hat y)$ w.r.t.\ $\hat c_c$) with the
\emph{causal effect} $E_c$ of flipping $\hat c_c\!\to\!1-\hat c_c$ and re-running $h$.
We report \textbf{reliance} (decision-prob drop when all concepts are neutralized to a
reference), \textbf{comprehensiveness}/\textbf{sufficiency} (ERASER-style, using $I$ to
rank concepts), and \textbf{ccf\_corr} $=\mathrm{Spearman}(I,E)$. A central point:
ccf\_corr is \emph{magnitude-blind}---a model whose concepts are causally inert has
$I_c\!\approx\!0$ and $E_c\!\approx\!0$ yet can be perfectly rank-correlated.
Faithfulness must be judged by magnitude-aware measures, as we show empirically.

\subsection{Fairness of reasoning}
On Fitzpatrick17k we compute accuracy and faithfulness per skin-tone group (I--II,
III--IV, V--VI), report worst-vs-best gaps, and test the dark-vs-light difference with
a bootstrap. As a retraining-free mitigation we fit a per-group temperature on concept
logits~\cite{guo2017} on a held-out split. We also contrast light-skin-only vs.\
skin-tone-diverse training regimes.

\section{Datasets and Protocol}
\textbf{derm7pt}~\cite{kawahara2019}: 1{,}011 cases with expert 7-point concept
annotations; official split (413 train, 203 val, 395 test); concepts binarized to the
score-positive variant. \textbf{Fitzpatrick17k}~\cite{groh2021}: 16{,}574 clinical images with
Fitzpatrick I--VI (V--VI: 2{,}168) and coarse three-way labels (benign, malignant, or
non-neoplastic); no concept ground truth. \textbf{PAD-UFES-20}~\cite{pacheco2020}: 2{,}298 smartphone
lesions; its \texttt{elevation} field gives partial clinical concept ground truth for
validating the bootstrap. \textbf{Leakage-clean protocol}~\cite{cassidy2022}: near-duplicates removed by
high-similarity clustering on frozen embeddings ($\approx$600 from Fitzpatrick17k) and
group-aware, diagnosis-stratified splits. PH2 was excluded (no mirror ships images and
concept labels together).

\section{Experiments}
Encoder: DermLIP (frozen) unless noted; heads are 1-hidden-layer MLPs. All results are
mean$\pm$std over 5 seeds; significance by paired Wilcoxon signed-rank (pure vs.\ leaky,
E2) or bootstrap difference (independent groups, E3).

\subsection{Diagnosis and concept accuracy}
We evaluate the concept-aligned detection task (Model A: melanoma; Model B: malignant).

\begin{table}[t]
\caption{Diagnosis and concept accuracy (detection); mean$\pm$std over 5 seeds.}
\label{tab:exp1}
\begin{center}
\begin{tabular}{llccc}
\hline\rule{0pt}{12pt}
Model & Variant & dx bal-acc & dx AUROC & concept AUROC\\[2pt]
\hline\rule{0pt}{12pt}
A & image-only & 0.777$\pm$0.016 & 0.856$\pm$0.025 & ---\\
A & CBM (ours) & 0.769$\pm$0.011 & 0.848$\pm$0.009 & 0.827$\pm$0.001\\
A & CBM-oracle (GT concepts) & \textbf{0.864$\pm$0.009} & \textbf{0.920$\pm$0.004} & ---\\
B & image-only & 0.796$\pm$0.008 & 0.893$\pm$0.005 & ---\\
B & CBM (ours) & 0.715$\pm$0.006 & 0.789$\pm$0.005 & (pseudo)\\[2pt]
\hline
\end{tabular}
\end{center}
\end{table}

As Table~\ref{tab:exp1} shows, for melanoma detection the bottleneck \emph{matches}
image-only (AUROC $0.848\pm0.009$ vs.\ $0.856\pm0.025$; statistically tied)---full
interpretability at no significant accuracy cost. With ground-truth concepts the oracle
reaches $0.920\pm0.004$, clearly exceeding image-only, so the concept set is highly
diagnostic; concepts are predicted stably (mean AUROC $0.827\pm0.001$), far above
zero-shot (0.644), quantifying the value of the foundation bootstrap. On the harder
clinical task Model B trails image-only by a modest margin ($0.789$ vs.\ $0.893$). The
clinical bootstrap validates externally against PAD-UFES \texttt{elevation} (AUROC
0.63); absolute size was not recoverable from crops (0.55) and was dropped.

\noindent\textbf{Concept completeness.} The concept layer captures nearly all of the
image's diagnostic signal: the concepts-only CBM retains $99\%$ of the image-only AUROC
($0.848/0.856$), and ground-truth concepts \emph{exceed} the full image ($0.920$ vs.\
$0.856$). The 7-point concept set is thus a near-complete---and, for melanoma,
superior---task representation, so bottlenecking through it sacrifices little.

\subsection{Faithfulness}
We compare the pure bottleneck against a \emph{leaky} CBM whose diagnosis head sees
concepts \emph{and} the embedding.

\begin{table}[t]
\caption{Concept-counterfactual faithfulness; mean$\pm$std over 5 seeds. Higher
reliance/comprehensiveness = more faithful; ccf\_corr (rank-only) is magnitude-blind.}
\label{tab:exp2}
\begin{center}
\begin{tabular}{llcccc}
\hline\rule{0pt}{12pt}
Model & Variant & reliance & compreh. & suffic. & ccf\_corr\\[2pt]
\hline\rule{0pt}{12pt}
A & pure bottleneck & \textbf{0.091$\pm$0.084} & \textbf{0.053$\pm$0.042} & 0.034 & 0.434\\
A & leaky & 0.000 & 0.000 & 0.000 & 0.856\\
B & pure bottleneck & \textbf{0.192$\pm$0.023} & \textbf{0.150$\pm$0.020} & 0.080 & 0.489\\
B & leaky & 0.107$\pm$0.019 & 0.103$\pm$0.018 & 0.022 & 0.756\\[2pt]
\hline
\end{tabular}
\end{center}
\end{table}

Table~\ref{tab:exp2} reports the results. The pure bottleneck relies on its concepts
significantly more than the leaky variant, by paired Wilcoxon signed-rank test
($p<10^{-7}$ for both models and both metrics). On the larger Fitzpatrick17k the effect
is large and stable (reliance $0.192\pm0.023$ vs.\ $0.107\pm0.019$); on the small
derm7pt the pure magnitude is seed-variable ($0.091\pm0.084$) but always far above the
near-inert leaky model ($\approx\!0$), whose decisions the concepts do not drive
(Fig.~\ref{fig:faith}). Strikingly, the leaky model scores \emph{higher} rank-agreement
(ccf\_corr 0.86 vs.\ 0.43 on Model A)---a concrete demonstration that rank-correlation
faithfulness is magnitude-blind and would certify an unfaithful model, motivating the
magnitude-aware measures. The result is not specific to the gradient attribution:
defining stated importance by leave-one-out instead gives essentially the same
comprehensiveness (Model~A $0.059$ vs.\ $0.053$; Model~B $0.143$ vs.\ $0.150$), and both
exceed a random-importance baseline (Model~B $0.150$ vs.\ $0.123$)---the stated-important
concepts are the more load-bearing ones.

\begin{figure}[t]
\centering
\includegraphics[width=0.9\textwidth]{faithfulness_bars.png}
\caption{Faithfulness by model and variant (mean$\pm$std, 5 seeds). The pure bottleneck
relies on its concepts significantly more than the leaky model (paired Wilcoxon
$p<10^{-7}$); the effect is large and stable on Fitzpatrick17k (Model B) and
seed-variable on the small derm7pt (Model A), where the leaky model is near-inert.
Magnitude-aware metrics expose the difference that rank-correlation misses.}
\label{fig:faith}
\end{figure}

\subsection{Fairness of reasoning}
\begin{table}[t]
\caption{Per-Fitzpatrick-group audit on Fitzpatrick17k (Model B), diverse-training
regime, mean$\pm$std over 5 seeds. Biased-regime V--VI shown for comparison.}
\label{tab:exp3}
\begin{center}
\begin{tabular}{llccc}
\hline\rule{0pt}{12pt}
Regime & Group & dx bal-acc & reliance & compreh.\\[2pt]
\hline\rule{0pt}{12pt}
Diverse & I--II & 0.710$\pm$0.011 & 0.185$\pm$0.017 & 0.146$\pm$0.019\\
Diverse & III--IV & 0.721$\pm$0.002 & 0.204$\pm$0.027 & 0.156$\pm$0.022\\
Diverse & V--VI & 0.692$\pm$0.012 & 0.192$\pm$0.038 & 0.154$\pm$0.024\\
Biased & V--VI & 0.687$\pm$0.013 & 0.169$\pm$0.010 & 0.143$\pm$0.006\\[2pt]
\hline
\end{tabular}
\end{center}
\end{table}

Table~\ref{tab:exp3} reports the per-skin-tone audit. \emph{Reasoning faithfulness is
statistically equitable across skin tones}: the dark-vs-light difference is not
significant in \emph{either} training regime (e.g.\ diverse
$\Delta_{\text{V--VI}-\text{I--II}}$ reliance not significant by bootstrap). To our
knowledge this is the first audit of whether concept-based reasoning is equally
faithful across skin tones, and the finding is reassuring: dark skin is not
disadvantaged. We further find that \emph{neither} skin-tone-diverse training (V--VI
reliance $0.169\pm0.010$ biased vs.\ $0.192\pm0.038$ diverse; overlapping) \emph{nor}
group-conditional concept calibration (gaps unchanged) produces a significant change---
we report both as honest negative results. The absence of a disparity is consistent
with the derm foundation encoder being largely skin-tone-robust; effect sizes are small
and dark-skin variance is higher (V--VI $n=407$), which we note as a limitation.

\subsection{Encoder ablation and qualitative}
\begin{table}[t]
\caption{Encoder ablation (Model A, melanoma detection); mean$\pm$std over 5 seeds.}
\label{tab:exp4a}
\begin{center}
\begin{tabular}{lccc}
\hline\rule{0pt}{12pt}
Encoder & dx AUROC & concept AUROC & reliance\\[2pt]
\hline\rule{0pt}{12pt}
DINOv2 (general) & 0.845$\pm$0.004 & 0.781$\pm$0.001 & 0.118$\pm$0.023\\
DermLIP (derm) & 0.848$\pm$0.009 & \textbf{0.827$\pm$0.001} & 0.091$\pm$0.084\\[2pt]
\hline
\end{tabular}
\end{center}
\end{table}

As Table~\ref{tab:exp4a} shows, at essentially identical diagnosis AUROC ($0.848$ vs.\
$0.845$) the dermatology encoder predicts the dermatologist concepts markedly more
accurately ($0.827\pm0.001$ vs.\ $0.781\pm0.001$; non-overlapping); reasoning
faithfulness (reliance) is comparable across encoders. Domain pretraining thus improves
concept \emph{prediction} specifically---and, via its text tower, enables the
pseudo-label bootstrap. Fig.~\ref{fig:qual} shows a melanoma case with predicted
concepts and the counterfactual concept whose flip most changes the decision.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{qualitative_melanoma.png}
\caption{Concept-counterfactual explanation for a melanoma case: predicted concept
probabilities (left) and each concept's causal effect on the decision (right); the
decisive concept is highlighted.}
\label{fig:qual}
\end{figure}

\section{Discussion and Limitations}
Interpretability claims should be tested, not assumed, and the test is cheap when the
model is a genuine bottleneck---as our robust pure-vs-leaky contrast shows. On fairness,
reasoning faithfulness was statistically equitable across skin tones in every regime we
tested; we found no significant disparity, and neither training diversity nor
calibration significantly changed the (already small) differences. This is a reassuring
negative result rather than a demonstrated mitigation. Its effect sizes are small,
partly because the derm foundation encoder is already largely skin-tone-robust, and
dark-skin variance is higher ($n=407$). A second honest caveat: the faithfulness
\emph{magnitude} on the small derm7pt (413 training cases) is seed-sensitive, though the
qualitative contrast (faithful bottleneck vs.\ inert leaky model) holds at every seed;
on the larger Fitzpatrick17k the magnitude is stable. Clinical concept pseudo-labels are
noisy and only partially validated (PAD-UFES elevation, AUROC 0.63); we treat
pseudo-label quality as a studied variable. Our metric is defined within the bottleneck;
extending it to end-to-end models and multi-way diagnosis, and a human study of
explanation plausibility, are future work.

\section{Conclusion}
Faithful-by-Concept makes concept-based dermatology AI verifiable and lets us ask
whether it is equitable. It tests, rather than assumes, that the concepts an
explanation cites causally drive the diagnosis; shows that a widely-used class of
``interpretable'' models fails this test while a pure bottleneck passes it; and
delivers the first audit of whether that faithfulness is fair across skin tones,
finding reasoning statistically equitable. The framework is cheap, reproducible, and
single-GPU.

%
% ---- Bibliography ----  (LNCS style; entries verified against sources)
%
\begin{thebibliography}{18}

\bibitem{koh2020}
Koh, P.W., Nguyen, T., Tang, Y.S., Mussmann, S., Pierson, E., Kim, B., Liang, P.:
Concept bottleneck models. In: ICML (2020)

\bibitem{bie2024mica}
Bie, Y., Luo, L., Chen, H.: MICA: towards explainable skin lesion diagnosis via
multi-level image-concept alignment. In: AAAI, vol.~38(2), pp.~837--845 (2024)

\bibitem{patricio2024}
Patr\'icio, C., Neves, J.C., Teixeira, L.F.: Towards concept-based interpretability of
skin lesion diagnosis using vision-language models. In: IEEE ISBI (2024)

\bibitem{yuksekgonul2023}
Yuksekgonul, M., Wang, M., Zou, J.: Post-hoc concept bottleneck models. In: ICLR (2023)

\bibitem{kim2024monet}
Kim, C., Gadgil, S.U., DeGrave, A.J., Omiye, J.A., Cai, Z.R., Daneshjou, R., Lee, S.-I.:
Transparent medical image AI via an image-text foundation model grounded in medical
literature. Nat. Med. 30(4), 1154--1165 (2024)

\bibitem{degrave2023}
DeGrave, A.J., Cai, Z.R., Janizek, J.D., Daneshjou, R., Lee, S.-I.: Auditing the
inference processes of medical-image classifiers by leveraging generative AI and the
expertise of physicians. Nat. Biomed. Eng. (2023)

\bibitem{deyoung2020}
DeYoung, J., Jain, S., Rajani, N.F., Lehman, E., Xiong, C., Socher, R., Wallace, B.C.:
ERASER: a benchmark to evaluate rationalized NLP models. In: ACL, pp.~4443--4458 (2020)

\bibitem{jacovi2020}
Jacovi, A., Goldberg, Y.: Towards faithfully interpretable NLP systems: how should we
define and evaluate faithfulness? In: ACL, pp.~4198--4205 (2020)

\bibitem{adebayo2018}
Adebayo, J., Gilmer, J., Muelly, M., Goodfellow, I., Hardt, M., Kim, B.: Sanity checks
for saliency maps. In: NeurIPS (2018)

\bibitem{daneshjou2022}
Daneshjou, R., Vodrahalli, K., Novoa, R.A., et al.: Disparities in dermatology AI
performance on a diverse, curated clinical image set. Sci. Adv. 8(31), eabq6147 (2022)

\bibitem{lu2022}
Lu, C., Lemay, A., Chang, K., Hoebel, K., Kalpathy-Cramer, J.: Fair conformal predictors
for applications in medical imaging. In: AAAI, vol.~36(11), pp.~12008--12016 (2022)

\bibitem{groh2021}
Groh, M., Harris, C., Soenksen, L., Lau, F., Han, R., Kim, A., Koochek, A., Badri, O.:
Evaluating deep neural networks trained on clinical images in dermatology with the
Fitzpatrick 17k dataset. In: CVPR Workshops (ISIC), pp.~1820--1828 (2021)

\bibitem{kawahara2019}
Kawahara, J., Daneshvar, S., Argenziano, G., Hamarneh, G.: Seven-point checklist and
skin lesion classification using multitask multimodal neural nets. IEEE J. Biomed.
Health Inform. 23(2), 538--546 (2019)

\bibitem{pacheco2020}
Pacheco, A.G.C., et al.: PAD-UFES-20: a skin lesion dataset composed of patient data
and clinical images collected from smartphones. Data Brief 32, 106221 (2020)

\bibitem{oquab2024}
Oquab, M., Darcet, T., Moutakanni, T., et al.: DINOv2: learning robust visual features
without supervision. Trans. Mach. Learn. Res. (TMLR) (2024)

\bibitem{dermlip2025}
Yan, S., et al.: Derm1M: a million-scale vision-language dataset aligned with clinical
ontology knowledge for dermatology. In: ICCV, pp.~12681--12690 (2025)

\bibitem{guo2017}
Guo, C., Pleiss, G., Sun, Y., Weinberger, K.Q.: On calibration of modern neural
networks. In: ICML, pp.~1321--1330 (2017)

\bibitem{cassidy2022}
Cassidy, B., Kendrick, C., Brodzicki, A., Jaworek-Korjakowska, J., Yap, M.H.: Analysis
of the ISIC image datasets: usage, benchmarks and recommendations. Med. Image Anal. 75,
102305 (2022)

\end{thebibliography}
\end{document}
